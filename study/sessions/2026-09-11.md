# 2026-09-11

## 晚间学习启动

- 用户请求按复习和学习计划继续。
- 实际学习时长：60 分钟（用户于 2026-09-15 补报确认）；不按聊天间隔自动累计。
- 入口检查触发：存在 09-08、09-09、09-10 已到期的错题复习节点。
- 今晚安排：Ethernet 逾期复测 20 分钟；海明码与 CRC 闭环 20 分钟；VLAN/STP 针对性训练 50 分钟，共 90 分钟。
- 本周最终确认投入 150 分钟，其中本次为 60 分钟；今晚原 90 分钟仅为计划预算。

## 第一组：Ethernet 逾期复测

- M-20260907-001 变式：数据率 `100 Mb/s`、最大站间距离 `1.5 km`、传播速度 `200 m/μs`，求满足 CSMA/CD 碰撞检测条件的理论最小帧长。要求写单向传播时延、往返传播时延、速率单位换算和最终 bit 数。
- M-20260907-002 变式：分别判断 `0x05DC` 与 `0x86DD` 应按 Length 还是 Type 解释，并说明数值或协议含义。
- 用户作答与结果：
  - CSMA/CD 四步均正确：`τ=7.5 μs`、`2τ=15 μs`、`100 Mb/s=100 bit/μs`、理论最小帧长 `1500 bit`。反向校验为 `1500 bit ÷ 100 bit/μs = 15 μs = 2τ`。
  - `0x05DC`、`0x86DD` 分别判断为 Length、Type，方向正确；但十进制换算写成 1100、34165。正确值分别为 1500、34525；`0x86DD` 应准确识别为 IPv6 EtherType。
- 复习结论：M-20260907-001 掌握度升至 2；M-20260907-002 部分通过，掌握度保持 1；下一节点均为 2026-09-14。

## 第二组：海明码与 CRC

- Hamming(7,4) 变式：采用偶校验，收到码字 `1010111`；要求计算 `S1`、`S2`、`S4`、综合症、错误位置、纠正后码字，并提取第 3、5、6、7 位原始数据。
- CRC 判断变式：接收端用约定生成多项式对完整接收码字回除，余数为 `000`；要求判断能否推出“没有发生比特错误”“没有丢包”“接收数据一定正确”，并写出 CRC 能严格支持的结论。
- 用户作答与结果：
  - 海明码全部正确：`S1=0`、`S2=1`、`S4=1`，综合症 `110₂=6`，翻转第 6 位后为 `1010101`，提取原始数据 `1101`。
  - CRC 判断方向正确：不能证明绝对无比特错误，也不能证明没有丢包；严格表述应为“对当前收到的码字，CRC 未检测到该生成多项式能够检测出的比特差错”。
  - 边界修正：TCP 可利用序号、确认与重传发现并处理其字节流中的缺失，但丢失检测并非只能由 TCP 完成；不同层协议也可能提供序号、确认或超时机制。
- 复习结论：M-20260902-001、M-20260902-002、M-20260902-003 的逾期节点已补做，掌握度均升至 3；下一次抽查为 2026-09-16。
- 补充自省：用户能定位 Length/Type 换算错误为把 `16²` 误算成 196，M-20260907-002 掌握度升至 2。

## 第三组：VLAN、STP 与链路聚合诊断

- 题目覆盖：MAC 地址学习与未知单播泛洪、Trunk 允许 VLAN 故障、STP 根桥选举、根端口路径开销、LACP 聚合吞吐与成员链路故障。
- 状态：等待用户完成五道原创场景题；答案提交后按知识点拆分反馈，不以总分掩盖薄弱点。
- 结算说明：本次实际学习 60 分钟，未完成的 VLAN/STP 内容转入后续动态安排，不机械补足原计划的 90 分钟。
